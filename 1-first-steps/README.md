## Description
It creates 3 tasks and just prints them with `fmt`

## Key points
* struct
* array of struct
* fmt.Stringer interface
* for ... range
* converting digit to string

## Makefile
* `make run` - run code
